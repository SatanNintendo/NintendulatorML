name: Build NintendulatorML

on:
  workflow_dispatch:  # ← только ручной запуск!

jobs:
  build:
    runs-on: windows-2019  # v141_xp уже установлен на этом образе

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Build project
      run: |
        msbuild msvc140/Nintendulator.vcxproj `
          /p:Configuration=Release `
          /p:Platform=Win32 `
          /p:PlatformToolset=v141_xp `
          /p:WindowsTargetPlatformVersion=8.1

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: NintendulatorML
        path: |
          Win32/Release/*.exe
          Win32/Release/*.dll
          Lang/*.lng
