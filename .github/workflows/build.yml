name: Build NintendulatorML

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2019

    strategy:
      matrix:
        platform: [Win32, x64]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Build Win32 + x64
      run: |
        echo "Building ${{ matrix.platform }}..."
        
        msbuild msvc140/Nintendulator.vcxproj /t:Rebuild /m:2 `
          /p:Configuration=Release `
          /p:Platform=${{ matrix.platform }} `
          /p:PlatformToolset=v140 `
          /p:WindowsTargetPlatformVersion=10.0 `
          /p:UseMultiToolTask=true `
          /verbosity:normal

    - name: Prepare release
      run: |
        New-Item -ItemType Directory -Force -Path release
        Copy-Item "msvc140/${{ matrix.platform }}/Release/*.exe" release/ -Force
        Get-ChildItem -Recurse -Filter *.dll | Copy-Item -Destination release/ -Force
        Copy-Item Lang release/Lang -Recurse -Force

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: NintendulatorML-${{ matrix.platform }}
        path: release
